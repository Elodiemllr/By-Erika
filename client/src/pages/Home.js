import React from "react";
import NavBar from "../components/NavBar.js";

const Home = () => {
    return (
        <div className="home">
            <NavBar />
        </div>
    );
};

export default Home;
